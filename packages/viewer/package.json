{
  "name": "@pplx-bridge/viewer",
  "version": "0.1.0",
  "scripts": {
    "build": "echo 'viewer is static HTML — no build step needed'",
    "dev":   "echo 'edit packages/viewer/public/live.html directly'"
  }
}
